name: Deploy Project Middle-East to GitHub Pages

on:
  push:
    branches:
      - main

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false
#  cancel-in-progress: true

jobs:
#  build:
#    runs-on: ubuntu-latest

#    steps:
#      - name: Checkout
#        uses: actions/checkout@v7

#      - name: Setup Node
#        uses: actions/setup-node@v7
#        with:
#          node-version: 20
#          cache: npm

#      - name: Install dependencies
#        run: npm ci

#      - name: Build website
#        run: npm run build
#
#      - name: Upload Pages artifact
#        uses: actions/upload-pages-artifact@v5
#        with:
#          path: public

  deploy:
#    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout
        uses: actions/checkout@v7

#        ## could be useful perhaps
#      - name: Setup Web Static Hosting Framework Page Assets
#        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: public
        
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
